server:
  port: 8080

spring:
  application:
    name: order-service

external:
  inventory-service:
    url: ${INVENTORY_URL:http://localhost:8081}
  payment-service:
    url: ${PAYMENT_URL:http://localhost:8082}